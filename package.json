{
  "scripts": {
    "build": "cd dashboard && npm install && npm run build",
    "backend": ".venv/bin/python cctv-backend/main.py",
    "dev": "nohup .venv/bin/python cctv-backend/main.py > backend.log 2>&1 & npm --prefix dashboard run dev"
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "react-leaflet": "^5.0.0",
    "tesseract.js": "^7.0.0"
  }
}

